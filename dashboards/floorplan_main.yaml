# dashboards/floorplan_main.yaml
views:
  - title: Seminterrato
    path: seminterrato
    panel: true
    cards:
      - type: 'custom:floorplan-card'
        config:
          image: /local/home-floorplan/home.svg
          stylesheet: /local/home-floorplan/floorplan.css
          vars:
            current_floor: "seminterrato"
          
          # Includiamo il file delle regole parametriche centralizzato
          rules: !include floorplan_rules.yaml

  - title: Piano Terra
    path: piano-terra
    panel: true
    cards:
      - type: 'custom:floorplan-card'
        config:
          image: /local/home-floorplan/home.svg
          stylesheet: /local/home-floorplan/floorplan.css
          vars:
            current_floor: "piano_terra"
          
          # Stesso identico file di regole per tutti
          rules: !include floorplan_rules.yaml


  # - title: Garage e Cancelli
  #   path: garage
  #   panel: true
  #   cards:
  #     - type: 'custom:floorplan-card'
  #       config:
  #         image: /local/home-floorplan/garage.svg
  #         stylesheet: /local/home-floorplan/floorplan.css
  #         config: dashboards/floorplan_config.yaml
  #         vars:
  #           current_floor: "garage"

  # - title: Seminterrato
  #   path: seminterrato
  #   panel: true
  #   cards:
  #     - type: 'custom:floorplan-card'
  #       config:
  #         image: /local/home-floorplan/home.svg
  #         stylesheet: /local/home-floorplan/floorplan.css
  #         config: dashboards/floorplan_config.yaml
  #         vars:
  #           current_floor: "seminterrato"

  # - title: Piano Terra
  #   path: piano-terra
  #   panel: true
  #   cards:
  #     - type: 'custom:floorplan-card'
  #       config:
  #         image: /local/home-floorplan/home.svg
  #         stylesheet: /local/home-floorplan/floorplan.css
  #         config: dashboards/floorplan_config.yaml
  #         vars:
  #           current_floor: "piano-terra"

  # - title: Piano Primo
  #   path: piano-primo
  #   panel: true
  #   cards:
  #     - type: 'custom:floorplan-card'
  #       config:
  #         image: /local/home-floorplan/home.svg
  #         stylesheet: /local/home-floorplan/floorplan.css
  #         config: dashboards/floorplan_config.yaml
  #         vars:
  #           current_floor: "piano-primo"

  # - title: Tetto
  #   path: tetto
  #   panel: true
  #   cards:
  #     - type: 'custom:floorplan-card'
  #       config:
  #         image: /local/home-floorplan/home.svg
  #         stylesheet: /local/home-floorplan/floorplan.css
  #         config: dashboards/floorplan_config.yaml
  #         vars:
  #           current_floor: "tetto"