# dashboards/floorplan_config.yaml
rules:
  - name: "Test Vita"
    entities:
      - sensor.time
    element: layer_seminterrato
    state_action:
      action: call-service
      service: floorplan.class_set
      service_data: >
        ${
          console.log("FLOORPLAN CONFIG LETTO - elemento:", element ? element.id : "NULL");
          return "layer-visible";
        }