# Questo file contiene la logica che collega l'SVG alle entità di HA
# Percorso nel repo: packages/dashboard_floorplan.yaml

lovelace:
  dashboards:
    floorplan-test: # Crea una dashboard dedicata nel menu laterale
      mode: yaml
      title: BOSCO TEST 123
      icon: mdi:floor-plan
      show_in_sidebar: true
      filename: dashboards/floorplan_main.yaml # Punto al file della dashboard
